#include"main.h"
/**
 * print_alphabet - utilizes on the _putchar function to print
 * the alphabet a -z
*/
void print_alphabet(void)
{
	int b;

	for (b = 'a'; b <= 'z'; ch++)
		_putchar(b);
	_putchar('\n');
}
