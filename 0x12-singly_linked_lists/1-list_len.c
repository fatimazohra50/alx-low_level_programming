#include "lists.h"
#include <stddef.h>
/**
 * list_len -returns the number of elements in a linked list_t list.
 */
size_t list_len(const list_t *h)
{
	size_t a;
	for (i = 0; h ; i++)
		h = h->next;
	return (a);
}

