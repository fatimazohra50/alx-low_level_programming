#include <stdio.h>
/**
 * main - prints the alphabet in lowercase, followed by a new line.
 * Return: 0
 */
int main(void)
{
	char z;

	for (z = 'a'; z <= 'z'; z++)
	{
		putchar(z);
	}
	putchar('\n');
		return (0);
}
